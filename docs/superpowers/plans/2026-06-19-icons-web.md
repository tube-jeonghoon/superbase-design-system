# Plan 9 — `@superbase/icons` 데이터 패키지 + 웹 Icon + 문서 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SVG 패스 데이터를 담은 프레임워크 무관 `@superbase/icons` 패키지를 만들고, 그것을 소비하는 웹 `<Icon name>` 컴포넌트(@superbase/react)와 문서 Icon 페이지를 추가한다.

**Architecture:** `@superbase/icons`는 `iconPaths`(name→path `d`), `IconName` 유니온, `iconNames`, `ICON_VIEWBOX`만 export(데이터, tsc 빌드). `@superbase/react`의 `Icon`은 이 데이터를 받아 인라인 `<svg>`(stroke 라인 스타일)로 렌더한다. `@superbase/icons`는 react 번들에서 external로 두고 dependency로 선언한다. 문서에 Icon 페이지(전체 그리드)와 사이드바 항목을 추가한다.

**Tech Stack:** TypeScript 5(tsc), React 19, Vite 5(react 라이브러리), Vitest + jsdom + Testing Library, Next.js 15(docs), Changesets.

> 전제: v0.2 + 문서 리디자인이 `main`에 있음. `@superbase/react`는 vite 라이브러리(번들 `"use client"`), `rollupOptions.external`에 react류가 있음. docs `componentNav`는 9개. Node 22, pnpm 10.27.0, 단일 vite 5 핀.

> 아이콘 스타일: 모두 24×24 viewBox, stroke 라인(`fill="none"`, `stroke="currentColor"`, `stroke-width=2`, round cap/join). `iconPaths`는 path의 `d`만 담는다. (초기 패스는 단순 기하 기반이며, 머지 후 문서 Icon 페이지에서 시각 검수·정제한다.)

---

## File Structure

| 파일 | 책임 |
|---|---|
| `packages/icons/package.json` / `tsconfig.json` / `tsconfig.build.json` / `vitest.config.ts` | 패키지 설정 |
| `packages/icons/src/index.ts` (+ `index.test.ts`) | iconPaths/IconName/iconNames/ICON_VIEWBOX |
| `packages/icons/README.md` | 사용법 |
| `packages/react/src/Icon/Icon.tsx` (+ `Icon.test.tsx`) | 웹 Icon |
| `packages/react/src/index.ts` (수정) | Icon export |
| `packages/react/package.json` (수정) | `@superbase/icons` 의존 |
| `packages/react/vite.config.ts` (수정) | external에 `@superbase/icons` |
| `apps/docs/components/docs/componentNav.ts` (+ `.test.ts` 수정) | "icon" 항목 추가(→10개) |
| `apps/docs/app/components/icon/page.tsx` | Icon 문서 페이지 |
| `apps/docs/package.json` (수정) | `@superbase/icons` 의존 |
| `.changeset/icons-v0-2.md` | icons·react minor |

---

## Task 1: `@superbase/icons` 데이터 패키지 (TDD)

**Files:** Create `packages/icons/package.json`, `tsconfig.json`, `tsconfig.build.json`, `vitest.config.ts`, `src/index.ts`, `src/index.test.ts`, `README.md`

- [ ] **Step 1: `packages/icons/package.json`**

```json
{
  "name": "@superbase/icons",
  "version": "0.1.0",
  "type": "module",
  "publishConfig": { "access": "public" },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tube-jeonghoon/superbase-design-system.git",
    "directory": "packages/icons"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
```

- [ ] **Step 2: `packages/icons/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "noEmit": true, "types": ["node", "vitest/globals"] },
  "include": ["src"]
}
```

- [ ] **Step 3: `packages/icons/tsconfig.build.json`**

```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": false, "declaration": true, "outDir": "dist", "rootDir": "src" },
  "include": ["src"],
  "exclude": ["src/**/*.test.ts"]
}
```

- [ ] **Step 4: `packages/icons/vitest.config.ts`**

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: { globals: true },
});
```

- [ ] **Step 5: 실패 테스트 — `packages/icons/src/index.test.ts`**

```ts
import { iconPaths, iconNames, ICON_VIEWBOX } from "./index";

describe("icons data", () => {
  it("exposes 18 icons", () => {
    expect(iconNames).toHaveLength(18);
  });

  it("every icon has a non-empty path string", () => {
    for (const name of iconNames) {
      expect(typeof iconPaths[name]).toBe("string");
      expect(iconPaths[name].length).toBeGreaterThan(0);
    }
  });

  it("iconNames matches iconPaths keys", () => {
    expect([...iconNames].sort()).toEqual(Object.keys(iconPaths).sort());
  });

  it("uses a 24px viewBox", () => {
    expect(ICON_VIEWBOX).toBe("0 0 24 24");
  });
});
```

- [ ] **Step 6: 실패 확인** — Run: `pnpm --filter @superbase/icons test` → FAIL (`./index` 없음). (먼저 `pnpm install`로 devDeps 설치.)

- [ ] **Step 7: `packages/icons/src/index.ts`**

```ts
export const ICON_VIEWBOX = "0 0 24 24";

export const iconPaths = {
  "chevron-left": "M15 6l-6 6 6 6",
  "chevron-right": "M9 6l6 6-6 6",
  "chevron-up": "M6 15l6-6 6 6",
  "chevron-down": "M6 9l6 6 6-6",
  check: "M5 13l4 4 10-10",
  close: "M6 6l12 12M18 6L6 18",
  plus: "M12 5v14M5 12h14",
  minus: "M5 12h14",
  search: "M4 10a6 6 0 1 0 12 0 6 6 0 1 0-12 0zM20 20l-5.8-5.8",
  menu: "M4 7h16M4 12h16M4 17h16",
  info: "M3 12a9 9 0 1 0 18 0 9 9 0 1 0-18 0zM12 11v5M12 8h0.01",
  warning: "M12 4L3 20h18L12 4zM12 10v4M12 17h0.01",
  success: "M3 12a9 9 0 1 0 18 0 9 9 0 1 0-18 0zM8 12l3 3 5-5",
  error: "M3 12a9 9 0 1 0 18 0 9 9 0 1 0-18 0zM9.5 9.5l5 5M14.5 9.5l-5 5",
  star: "M12 3.5l2.6 5.3 5.8.8-4.2 4.1 1 5.8-5.2-2.7-5.2 2.7 1-5.8-4.2-4.1 5.8-.8z",
  heart: "M12 20l-1.1-1C6.6 15.2 4 12.9 4 9.9 4 7.6 5.8 6 7.9 6c1.4 0 2.8.7 3.6 1.9C12.3 6.7 13.7 6 15.1 6 17.2 6 19 7.6 19 9.9c0 3-2.6 5.3-6.9 9.1L12 20z",
  user: "M8 8a4 4 0 1 0 8 0 4 4 0 1 0-8 0zM5 20a7 7 0 0 1 14 0",
  settings: "M4 8h9M17 8h3M4 16h3M11 16h9M15 6v4M8 14v4",
} as const;

export type IconName = keyof typeof iconPaths;

export const iconNames = Object.keys(iconPaths) as IconName[];
```

- [ ] **Step 8: 통과 확인** — Run: `pnpm --filter @superbase/icons test` → PASS (4 tests).

- [ ] **Step 9: 빌드 + typecheck**

Run: `pnpm --filter @superbase/icons build` → `dist/index.js` + `dist/index.d.ts` 생성.
Run: `pnpm --filter @superbase/icons typecheck` → exit 0.

- [ ] **Step 10: `packages/icons/README.md`**

```markdown
# @superbase/icons

프레임워크 무관 아이콘 패스 데이터(단일 소스). `@superbase/react`·`@superbase/react-native`의 `Icon`이 소비한다.

- `iconPaths` — name → SVG path `d`
- `IconName` — name 유니온 타입
- `iconNames` — 전체 name 배열
- `ICON_VIEWBOX` — `"0 0 24 24"`

모든 아이콘은 24×24 stroke 라인 스타일. 추가는 `iconPaths`에 항목 1개를 더하면 된다.
```

- [ ] **Step 11: Commit**

```bash
git add packages/icons pnpm-lock.yaml
git commit -m "feat(icons): add @superbase/icons path data package"
```

---

## Task 2: 웹 Icon (@superbase/react, TDD)

**Files:** Create `packages/react/src/Icon/Icon.tsx`, `Icon.test.tsx`; Modify `packages/react/src/index.ts`, `packages/react/package.json`, `packages/react/vite.config.ts`

- [ ] **Step 1: react가 icons를 의존하도록 — `packages/react/package.json`의 `dependencies`(없으면 추가) 또는 적절 위치에 추가**

`"peerDependencies"` 블록 위(또는 객체 최상위)에 `dependencies`를 추가/병합한다. react/package.json에 `dependencies`가 없다면 아래를 추가:
```json
  "dependencies": {
    "@superbase/icons": "workspace:*"
  },
```
그리고 `devDependencies`에도 추가(워크스페이스 링크 + 타입):
```json
    "@superbase/icons": "workspace:*",
```
(devDependencies 객체의 알파벳상 적당한 위치에 한 줄.)

- [ ] **Step 2: vite external에 icons 추가 — `packages/react/vite.config.ts`의 `rollupOptions.external` 배열에 추가**

`external: [ "react", "react-dom", ... ]` 배열에 `"@superbase/icons"`를 추가한다(맨 앞 또는 적당한 위치):
```ts
      external: [
        "@superbase/icons",
        "react",
        "react-dom",
        "react/jsx-runtime",
        "react/jsx-dev-runtime",
      ],
```

- [ ] **Step 3: 설치**

Run: `pnpm install` → react가 `@superbase/icons` 워크스페이스를 링크.
Run: `pnpm turbo run build --filter=@superbase/icons` → icons dist 보장(react 타입체크가 icons 타입을 참조).

- [ ] **Step 4: 실패 테스트 — `packages/react/src/Icon/Icon.test.tsx`**

```tsx
import { render } from "@testing-library/react";
import { iconPaths } from "@superbase/icons";
import { Icon } from "./Icon";

describe("Icon", () => {
  it("renders an svg with the named path's d", () => {
    const { container } = render(<Icon name="check" />);
    expect(container.querySelector("path")?.getAttribute("d")).toBe(iconPaths.check);
  });

  it("applies size and color", () => {
    const { container } = render(<Icon name="check" size={32} color="red" />);
    const svg = container.querySelector("svg");
    expect(svg?.getAttribute("width")).toBe("32");
    expect(svg?.getAttribute("stroke")).toBe("red");
  });

  it("is aria-hidden by default and labeled when label is given", () => {
    const { container, rerender } = render(<Icon name="check" />);
    expect(container.querySelector("svg")?.getAttribute("aria-hidden")).toBe("true");
    rerender(<Icon name="search" label="검색" />);
    const labeled = container.querySelector('svg[role="img"]');
    expect(labeled?.getAttribute("aria-label")).toBe("검색");
  });
});
```

- [ ] **Step 5: 실패 확인** — Run: `pnpm --filter @superbase/react test` → FAIL (`./Icon` 없음).

- [ ] **Step 6: `packages/react/src/Icon/Icon.tsx`**

```tsx
import type { SVGProps } from "react";
import { iconPaths, ICON_VIEWBOX, type IconName } from "@superbase/icons";

export interface IconProps extends Omit<SVGProps<SVGSVGElement>, "name" | "color"> {
  name: IconName;
  size?: number;
  color?: string;
  label?: string;
}

export function Icon({
  name,
  size = 20,
  color = "currentColor",
  label,
  ...rest
}: IconProps) {
  const a11y = label
    ? ({ role: "img", "aria-label": label } as const)
    : ({ "aria-hidden": true } as const);
  return (
    <svg
      width={size}
      height={size}
      viewBox={ICON_VIEWBOX}
      fill="none"
      stroke={color}
      strokeWidth={2}
      strokeLinecap="round"
      strokeLinejoin="round"
      {...a11y}
      {...rest}
    >
      <path d={iconPaths[name]} />
    </svg>
  );
}
```

- [ ] **Step 7: 통과 확인** — Run: `pnpm --filter @superbase/react test` → PASS.

- [ ] **Step 8: `packages/react/src/index.ts` 끝에 추가**

```ts
export { Icon } from "./Icon/Icon";
export type { IconProps } from "./Icon/Icon";
```
(`IconName` 타입은 `@superbase/icons`에서 직접 가져다 쓰므로 여기서 재-export하지 않는다.)

- [ ] **Step 9: 빌드 + typecheck + commit**

Run: `pnpm --filter @superbase/react build` → 성공. `grep -n "@superbase/icons" packages/react/dist/index.js` 로 import가 external(번들에 인라인 안 됨)인지 확인 — `import` 구문으로 남아야 한다.
Run: `pnpm --filter @superbase/react typecheck` → exit 0.
```bash
git add packages/react/src/Icon packages/react/src/index.ts packages/react/package.json packages/react/vite.config.ts pnpm-lock.yaml
git commit -m "feat(react): add Icon component consuming @superbase/icons"
```

---

## Task 3: 문서 Icon 페이지 + 네비 + changeset + 검증

**Files:** Modify `apps/docs/components/docs/componentNav.ts`, `componentNav.test.ts`, `apps/docs/package.json`; Create `apps/docs/app/components/icon/page.tsx`, `.changeset/icons-v0-2.md`

- [ ] **Step 1: componentNav 테스트 길이 갱신 — `componentNav.test.ts`**

`expect(componentNav).toHaveLength(9);` 를 `expect(componentNav).toHaveLength(10);` 로 바꾼다.

- [ ] **Step 2: 실패 확인** — Run: `pnpm --filter @superbase/docs test` → FAIL (아직 9개).

- [ ] **Step 3: componentNav에 icon 추가 — `componentNav.ts`의 배열에서 `{ slug: "checkbox", label: "Checkbox" },` 다음에 추가**

```ts
  { slug: "icon", label: "Icon" },
```

- [ ] **Step 4: 통과 확인** — Run: `pnpm --filter @superbase/docs test` → PASS (componentNav 10).

- [ ] **Step 5: docs가 icons를 의존하도록 — `apps/docs/package.json`의 `dependencies`에 추가**

`"@superbase/react": "workspace:*",` 옆에 한 줄 추가:
```json
    "@superbase/icons": "workspace:*",
```
그 후 `pnpm install`.

- [ ] **Step 6: `apps/docs/app/components/icon/page.tsx`**

```tsx
"use client";
import { Icon } from "@superbase/react";
import { iconNames } from "@superbase/icons";
import { ComponentDoc } from "../../../components/docs/ComponentDoc";
import { Example } from "../../../components/docs/Example";
import { Code } from "../../../components/docs/Code";

export default function IconPage() {
  return (
    <ComponentDoc title="Icon" lead="자체 큐레이션한 라인 아이콘 세트. name으로 선택합니다.">
      <Example
        title="기본 사용"
        description={
          <>
            <Code>name</Code>으로 아이콘을, <Code>size</Code>·<Code>color</Code>로 크기와 색을
            정합니다. 기본 색은 텍스트 색을 따릅니다.
          </>
        }
        code={`<Icon name="search" />\n<Icon name="check" size={24} color="var(--color-brand-primary)" />`}
      >
        <Icon name="search" />
        <Icon name="check" size={24} color="var(--color-brand-primary)" />
      </Example>

      <Example
        title="전체 아이콘"
        description="현재 제공하는 모든 아이콘입니다."
        code={`import { iconNames } from "@superbase/icons";\niconNames.map((name) => <Icon name={name} />)`}
      >
        <div
          style={{
            display: "grid",
            gridTemplateColumns: "repeat(6, 1fr)",
            gap: "var(--spacing-4)",
            width: "100%",
          }}
        >
          {iconNames.map((n) => (
            <div
              key={n}
              style={{
                display: "flex",
                flexDirection: "column",
                alignItems: "center",
                gap: "var(--spacing-1)",
              }}
            >
              <Icon name={n} size={24} />
              <span style={{ fontSize: "var(--font-size-caption)", color: "var(--color-text-secondary)" }}>
                {n}
              </span>
            </div>
          ))}
        </div>
      </Example>
    </ComponentDoc>
  );
}
```

- [ ] **Step 7: changeset — `.changeset/icons-v0-2.md`**

```markdown
---
"@superbase/icons": minor
"@superbase/react": minor
---

자체 큐레이션 아이콘 데이터 패키지 @superbase/icons를 추가하고, 이를 소비하는 웹 Icon 컴포넌트를 @superbase/react에 추가한다.
```

- [ ] **Step 8: 전체 검증**

Run: `pnpm turbo run typecheck test build`
Expected: icons·tokens·react·react-native·docs 전부 통과. docs `next build` 라우트 표에 `/components/icon`이 나타난다. react 테스트 +3(Icon), icons 테스트 4, docs 테스트 동일(컴포넌트nav 길이만 변경). 실제 수치 보고.

- [ ] **Step 9: Commit**

```bash
git add apps/docs/components/docs/componentNav.ts apps/docs/components/docs/componentNav.test.ts apps/docs/package.json apps/docs/app/components/icon .changeset/icons-v0-2.md pnpm-lock.yaml
git commit -m "feat(docs): add Icon page + nav; changeset for icons"
```

---

## 완료 기준 (Definition of Done)

- `pnpm turbo run typecheck test build`가 전부 통과한다.
- `@superbase/icons`가 18개 아이콘 패스 + `IconName`/`iconNames`/`ICON_VIEWBOX`를 export한다.
- `@superbase/react`가 `<Icon name>`을 export하고, `@superbase/icons`를 external 의존으로 소비한다(번들에 인라인 안 됨).
- 문서 사이트에 `/components/icon` 페이지(샘플 + 전체 그리드)와 사이드바 항목이 있다.
- `.changeset/icons-v0-2.md`가 icons·react를 minor로 예약한다.

## 다음 플랜

- **Plan 10**: `@superbase/react-native`에 `Icon`(react-native-svg) 추가 — `@superbase/icons` 동일 소비. changeset(react-native minor).
- 머지 후: 문서 Icon 페이지에서 18개 아이콘을 **시각 검수**하고, 어색한 패스(특히 star/heart/settings)를 정제.
